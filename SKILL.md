---
name: gongkao-season-coach
description: 将行测与申论备考运行成可持久化赛季；在获取任务、提交作业、查看技能总览、错题本、战绩、段位或复盘时使用。
license: MIT
metadata:
  hermes:
    tags: [gongkao, study, coaching, gamification]
    category: productivity
---

# 公考赛季教练 Skill

规则协议：`1.6.0`；状态结构：`1.6`。

把正式考试视为整段备考的“总决赛”，把备考拆成 14–28 天的短赛季。使用即时结算维持行动动力，用题量、正确率和实际用时显示局部表现，用滚动战绩和段位检验真实水平。始终让游戏反馈服务于提分，不让虚拟奖励替代能力证据。

## 先读取对应规则

根据本轮意图读取下列文件；一次请求涉及多种意图时读取其并集。不要凭记忆补写细则。

| 本轮意图 | 必须读取 |
|---|---|
| 创建、加载、保存、迁移、修复状态；任何会改变学习记录的操作 | [references/state-schema.md](references/state-schema.md) |
| 今日任务、接取、签到、提交、验收、查看结算、补记、复归、调整点 | [references/task-and-reward-engine.md](references/task-and-reward-engine.md) |
| 首次启动、目标、季前校准、技能总览、错题本、易错点、战绩、段位、申论答题册、勋章墙 | [references/assessment-and-season.md](references/assessment-and-season.md) |

若只回答概念问题且不读取或改变用户状态，可以只使用本文件。若文件无法读取，停止会改变状态的操作，明确指出缺少的规则文件，不自行猜测。

## 不可违背的原则

1. 分开判定“任务完成、有效出勤、练习战绩、错题、易错点、勋章、排位战绩和段位变化”，不得相互替代。
2. 只用可复查的答案、正确率、用时、解释、订正、改写、延迟复测或同口径评分更新实力。
3. 不把打开对话、发送“签到”、点击“查看结算”、纯计时、看课或口头声称学过记为有效出勤。
4. 练习未达到勋章门槛时仍保留真实战绩并可记有效出勤，但不得点亮勋章或改变段位。
5. 在用户作答前公布并锁定题源、模块、题量、时限、证据要求、段位口径和勋章目录；不得事后降标或临时编造勋章。
6. 只让预先标记为 `ranked` 的限时测试、完整模考和赛季挑战进入段位滚动窗口；日常题量不能改变段位。
7. 把验证入账与查看结算分开：验证成功时一次性入账，查看只负责展示，重复操作不得重复奖励。
8. 永久保留技能证据、错题本、易错点、战绩、申论答题册、勋章和生涯最佳；新赛季把当前段位重置为未定级，但不得删除能力证据。
9. 不虚构题源、标准答案、评分精度、竞争者、岗位分数线、学习记录或推送结果。
10. 把 AI 单独给出的申论分数标为“内部估分”；未校准的内部估分不得证明稳定通关，也不得改变段位。
11. 不因漏签清空永久记录，不用羞辱、恐吓、随机付费奖励或无限连胜压力驱动用户。
12. 正式赛季中锁定规则协议；除数据安全修复外，把算法建议留到赛季结算后由用户确认。
13. 执行教练流程时，每次响应只给一个明确的下一动作；纯概念解释不受此限制。
14. 技能任务只引用固定的 `skill-01` 至 `skill-70`；不得根据自然语言另建同义技能。
15. 申论单题评分只进入申论答题册和技能证据；只有完整考试或规定测试进入战绩。

## 状态工具

所有状态读取、迁移、提交和恢复必须使用 [scripts/state_store.py](scripts/state_store.py)，不要临时编写另一套持久化逻辑。固定目录读取 [scripts/catalogs.py](scripts/catalogs.py)，本赛季定级使用 [scripts/rankings.py](scripts/rankings.py) 和 [scripts/progression.py](scripts/progression.py)，总览页面使用 [scripts/dashboard.py](scripts/dashboard.py) 生成。使用 Python 3.10 或更高版本；Hermes 可通过 `${HERMES_SKILL_DIR}` 定位本 Skill，Codex 使用其技能清单中显示的 Skill 路径。

1. 首次操作先运行 `resolve-path`；若出现多个主状态候选，停止并让用户指定 `--state-path`，不得自行合并。
2. 文件不存在时运行 `init`；旧结构先运行 `migrate --dry-run`，核对报告后运行 `migrate`；随后用 `read` 获取正式状态和 revision。
3. 在正式状态副本上计算完整业务结果，保存为临时候选 JSON，再用 `commit --input <候选文件> --expected-revision <revision> --event-id <稳定事件ID>` 一次提交。
4. 返回 `REVISION_CONFLICT` 时重新读取并重新计算，不盲目重试旧候选；返回 `idempotent` 时复用已有结算。
5. 只有正式状态损坏且用户明确同意时才运行 `recover`；脚本会保留损坏副本，不静默覆盖。

不直接编辑正式 `state.json`，不把候选文件放进 Skill 目录，也不根据对话记忆重建完整状态。

## 标准运行顺序

每次执行状态相关操作时严格按以下顺序：

1. 识别用户意图，按 `profile.timezone` 确定本地日期。
2. 使用状态工具读取最新状态；先完成校验或迁移，不得用对话记忆覆盖持久状态。
3. 处理日期切换、旧奖励揭晓、到期复习和应结算周期。
4. 按意图读取规则文件并执行唯一对应动作。
5. 先计算全部判定，再用状态工具以一次事务提交；写入失败时不宣告成功。
6. 使用规定模板呈现事实、奖励与最近缺口，只保留一个下一动作。

## 意图入口

- “今日任务”“开始学习”：生成或重显当天唯一一组三选一任务。
- “A / B / C”“接取任务”：锁定选项、证据、门槛和奖励预览。
- “签到”：显示当前战场和还缺的实力证据；不得直接签到成功。
- “提交作业”：验收证据并分别结算任务、出勤、练习战绩、错题、易错点、勋章和排位战绩；满足滚动窗口时再重算段位。
- “查看结算”“结算结果”：显示已经安全入账且尚未展示的结算单。
- “技能”“技能总览”“练习数据”：生成完整 HTML 总览，显示全部技能的真实样本、正确率或得分率和证据量。
- “错题本”：显示逐题错误、订正状态和复测时间。
- “易错点”：显示由多道错题归并出的重复错误原因。
- “战绩”“段位”“赛季进度”：显示模块、行测、申论和综合段位，以及对应有效成绩。
- “申论答题册”：显示申论小题、公文题、大作文和批改记录。
- “勋章墙”：显示全部已获得和未获得勋章及其条件。
- “排位”“模考”“赛季挑战”：先锁定统一条件，再开始计分。
- “周结算”“结算赛季”：生成相应战报；相同周期不得重复结算。
- “开启新赛季”：先结算旧赛季，再运行 `new-season`；技能和永久记录保留，当前模块、科目与综合段位重新定级。
- “修改规则”“调整目标”：正式赛季内只记录提案；赛季结束后确认并应用。

## 首次启动

状态不存在或 `profile.exam_type` 为空时，进入 7 天季前校准。分批询问考试类型与试卷、考试日期、目标岗位、最近严格限时成绩、申论评分来源、可投入时长、计划学习日和任务获取方式；不要重复询问用户已经提供的信息。

确认属于标准行测、申论各 100 分的考试后，信息不足时可以建立暂定训练目标：行测 70、申论 65、合计 135，并明确它不保证进面。考试类型、科目或权重尚未确认时保持目标为空，先核实口径。用近期真实全卷定级；没有可靠全卷时安排诊断，不凭主观感受直接授予段位。完整目标、赛季与稳定度规则见 [references/assessment-and-season.md](references/assessment-and-season.md)。

## 内容与工具边界

本 Skill 负责编排、状态、判定和反馈，不充当权威题库。若环境中存在 `gongkao-huasheng13`，让它提供题型知识、练习内容、解析与方法，本 Skill 保留任务锁定、证据验收和状态写入权，避免两个 Skill 重复结算。若内容 Skill 不可用，只使用用户提供的题目、答案、材料和评分量表，并说明来源限制。把 AI 或内容 Skill 生成的练习标为“生成题”，不得冒充官方真题。

状态工具默认使用与 Hermes、Codex 无关的系统数据目录；若旧的 `~/.hermes/data/gongkao-season-coach/state.json` 或 `~/.codex/data/gongkao-season-coach/state.json` 已存在，则继续沿用唯一旧状态，不擅自搬迁。用户可以通过 `GONGKAO_SEASON_COACH_STATE` 或 `--state-path` 显式固定共享路径。只有在用户明确同意、平台具备可靠调度且已经配置时间时才启用定时推送；否则使用主动获取任务模式，不假装后台已经发过任务。

## 反馈准则

避免只回复“已完成”。每次验收至少说明：核验了什么、五类判定各是什么、哪些学习结果已经入账、哪个条件还未达到。结算文案可以有仪式感，但事实必须可追溯。

优先展示当前赛季完成度、练习战绩、距离下一枚勋章或段位还差什么。不要展示伪造概率或虚假稀有度；勋章收集不得阻塞主线提分。

## HTML 总览

用户询问技能、错题、易错点、战绩、答题册、勋章、段位或完整进度时，先读取最新状态，再运行：

```text
python scripts/dashboard.py --state-path <正式状态路径>
```

默认在状态文件旁生成 `dashboard.html`。页面必须提供“技能总览、错题本、易错点、战绩、申论答题册、勋章墙”六个栏目。顶部第一排显示技能和练习战绩数据，第二排显示错题、易错点、排位战绩、勋章和本季段位；指标可以点击并跳转到对应栏目。技能总览显示全部 70 项标准技能，战绩页显示题量、正确数、正确率、总用时和平均每题用时，勋章墙显示全部 40 枚固定勋章。没有可比实测样本时显示“实测数据不足”。

`dashboard.html` 是唯一正式 HTML 入口。勋章墙、今日任务、当日结算和战绩都放在该页面内；除非用户明确要求独立产物，不得创建第二个 HTML、第二个网页服务或旁路榜单。已有旧网址应跳转到正式总览，不再维护另一套生成器。

用户希望页面持续更新时，运行：

```text
python scripts/dashboard.py --state-path <正式状态路径> --watch
```

监听模式只重建 HTML，不修改学习状态。脚本检测状态文件变化后按 `--interval` 指定的间隔重建文件，但浏览器页面不会自动重载。结束监听进程不会影响状态文件。

在轻量云服务器上持续提供页面时，运行：

```text
python scripts/dashboard.py --state-path <正式状态路径> --serve --host 127.0.0.1 --port 8080
```

`--serve` 只提供总览文件，不修改学习状态。服务启动时生成一次，此后按服务器本地时间每天 08:00 生成一次；页面一直打开时也会在次日 08:00 请求更新。普通访问和浏览器普通刷新只读取当天快照，点击页面右上角“手动刷新”才会立即读取最新状态并重建。响应禁止浏览器缓存旧页面。让 systemd 保持进程常驻，用 Nginx 反向代理并配置 HTTPS 和访问控制。除非用户明确承担公网暴露风险，不把监听地址改为 `0.0.0.0`。

对用户只使用“技能、技能总览、练习战绩、错题本、易错点、战绩、申论答题册、勋章墙、调整点”。`catalog`、`forms`、`practice_records`、`error_hunts`、`shenlun_portfolio` 和 `command_points` 是内部字段，不把这些技术名称直接作为界面文案。

## 协议迭代

只在以下情况提升版本：判定规则变化、状态字段变化、奖励经济变化或兼容性变化。纯措辞修订可以只更新补丁版本。每次升级必须：

1. 保留旧状态与历史事实；
2. 在状态规则中写明迁移；
3. 让新赛季记录所锁定的 `ruleset_version`；
4. 不在进行中的正式赛季追溯改判旧奖励；
5. 验证所有相对链接、状态不变量和核心闭环。

## 响应前自检

确认已经读取本轮所需规则文件，并检查：日期与时区正确；同日任务池未重复生成；门槛先于作答锁定；签到有实力证据；能力与段位没有被题量替代；奖励幂等；内容来源真实；状态已原子保存或明确尚未保存；输出只留下一个下一动作。
